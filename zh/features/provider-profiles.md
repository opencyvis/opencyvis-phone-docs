# Provider 配置方案

保存多套 AI 提供商配置，一键切换。

## 使用场景

- **工作**：通过公司 API 网关使用 Qwen 3.5 Plus
- **家里**：通过 Ollama 本地运行 Gemma 4（完全私密）
- **复杂任务**：使用 Claude Opus 4（推理质量最高）

不需要每次切换都重新输入 API 地址和密钥。创建一次，之后在设置的下拉菜单中选择即可。

## 分享配置方案

任何配置方案都可以导出为二维码（**设置 → LLM Provider → 分享**），在其他设备上扫码即可一键导入完整配置，无需手动填写。
